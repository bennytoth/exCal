const CONFIG = {
    // Webhook to prepare new Event (from marked text)
    //API_URL: "http://localhost:5678/webhook-test/calendar-parse",
    API_URL: "https://unrenunciable-illusive-kanesha.ngrok-free.dev/webhook/calendar/prepare",
    // Webhook to confirm new Event (cofirm with button)
    CONFIRM_EVENT_URL: "https://unrenunciable-illusive-kanesha.ngrok-free.dev/webhook-test/calendar/create",
    // Webhook für alle gespeicherten Events abrufen
    EVENTS_API_URL: "http://localhost:5678/webhook-test/calendar-events"
};